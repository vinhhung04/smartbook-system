apiVersion: 1

providers:
  - name: SmartBook
    orgId: 1
    folder: SmartBook
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards

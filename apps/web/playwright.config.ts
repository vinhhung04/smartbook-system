import { defineConfig, devices } from '@playwright/test';

/**
 * Golden-path UI smoke tests against a running SmartBook stack. Assumes the API Gateway
 * (and everything behind it) is already up — see e2e/README.md. These are not unit tests
 * and don't start their own dev server: point BASE_URL at whatever's already running.
 */
export default defineConfig({
  testDir: './e2e',
  fullyParallel: false,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 1 : 0,
  workers: 1,
  reporter: process.env.CI ? [['list'], ['html', { open: 'never' }]] : 'list',
  timeout: 60_000,
  use: {
    baseURL: process.env.BASE_URL || 'http://localhost:5173',
    trace: 'retain-on-failure',
    screenshot: 'only-on-failure',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
});
